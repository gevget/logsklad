# 33 — DEMO SCENARIOS & ACCEPTANCE TESTS

## 1. Назначение

Этот документ фиксирует, что именно должно работать в Demo MVP и как проверять готовность проекта.

Demo считается успешным, если заказчик может увидеть один и тот же реальный бизнес-процесс глазами разных ролей:

**Client → Manager → Driver → Warehouse → Manager → Client**

Все сценарии должны работать на общей БД и изменять реальные данные, а не переключать заранее нарисованные состояния.

---

## Фактический прогон 24.09.2026

| Сценарий | Сохранённый результат | Статус проверки |
|---|---|---|
| A · полный складской цикл | TR-20260924-9907C3, от заявки клиента до «Завершена»; 24 500 ₽, складские операции, адрес выдачи и Proof of Delivery | Пройден через UI на локальной PostgreSQL |
| B · прямая доставка и проблема | TR-20260924-81910A; склад не участвует, водитель сообщил о проблеме с фото, менеджер продолжил маршрут, заказ завершён | Пройден; клиент получил статусные уведомления |
| C · транспортная компания | TR-20260924-95E41E; перевозчик, плательщик, Казань, услуги и вложение сохранены | Форма и карточка проверены; SUBMITTED |
| D · документы | TR-20260924-7EBB09; комплекты, возврат документов, маршрут и вложение сохранены | Форма и карточка проверены; SUBMITTED |
| Права, файлы, уведомления | Чужой заказ закрыт клиенту; внутренняя информация доступна менеджеру и закрыта клиенту; Proof of Delivery принимает назначенный водитель; 11/11 локальных объектов существуют и совпадают по размеру; три 68-байтовые заглушки заменены валидными 960×600 PNG, API отдаёт 200 | Проверено на локальной базе |
| Responsive и типографика | 33 маршрута × 9 размеров = 297 сочетаний; нет горизонтального overflow/обрезанных metric-карточек; минимум видимого текста 13 px | Пройдено после правки сетки |
| Release gates | pnpm lint, pnpm typecheck, pnpm build | Пройдены |

Инструкция по локальному показу: [38_LOCAL_DEMO_RUNBOOK.md](38_LOCAL_DEMO_RUNBOOK.md). Облачное развёртывание и фактическое восстановление seed остаются отдельными задачами; сброс здесь не выполнялся.

---

# 2. Главный Demo Flow A — Полный цикл через склад

## Стартовые условия

Есть:
- Client: Алексей Морозов;
- Company: ООО «СтальПром»;
- Manager: Анна Смирнова;
- Driver: Сергей Волков;
- Vehicle: Газель / seeded vehicle;
- Warehouse: Основной склад;
- поставщик;
- тестовый груз.

---

## A1. Client создаёт заявку

Client:
1. переключается на роль Client;
2. открывает `Создать заявку`;
3. выбирает `Забрать груз и привезти на склад`;
4. выбирает / создаёт поставщика;
5. указывает адрес забора;
6. добавляет груз;
7. выбирает склад;
8. выбирает услуги:
   - разгрузка;
   - упаковка;
   - фотоотчёт;
9. прикладывает документ;
10. открывает Review;
11. отправляет заявку.

### Acceptance

- [x] создаётся реальная запись Order;
- [x] создаются Cargo;
- [x] создаются RoutePoint;
- [x] сохраняются Services;
- [x] Attachment связан с Order;
- [x] Order получает `SUBMITTED`;
- [x] StatusHistory содержит событие;
- [x] Manager получает notification;
- [x] Client видит заявку в списке;
- [x] после refresh данные не исчезают.

---

## A2. Manager принимает заявку

Manager:
1. переключается на Manager;
2. видит заявку во `Входящих`;
3. открывает её;
4. переводит в `REVIEW`;
5. проверяет маршрут и груз;
6. задаёт стоимость;
7. подтверждает услуги;
8. переводит в `CONFIRMED`;
9. назначает Driver;
10. назначает Vehicle.

### Acceptance

- [x] Client data доступны Manager;
- [x] internal controls не видны Client;
- [x] цена сохраняется;
- [x] назначение Driver сохраняется;
- [x] назначение Vehicle сохраняется;
- [x] статус становится `DRIVER_ASSIGNED`;
- [x] Driver получает notification;
- [x] Client получает notification;
- [ ] Audit содержит назначение;
- [x] Timeline обновляется.

---

## A3. Driver забирает груз

Driver:
1. переключается на Driver;
2. видит назначенный рейс на главной;
3. открывает Job;
4. нажимает `Начать поездку`;
5. статус → `PICKUP_IN_PROGRESS`;
6. открывает адрес в карте / видит route data;
7. после погрузки прикладывает фото;
8. нажимает `Груз забран`;
9. статус → `PICKED_UP`.

### Acceptance

- [x] Driver видит только свои задания;
- [x] Driver не видит финансовую часть;
- [x] статус меняется только на разрешённый;
- [x] фото сохраняется;
- [x] StatusHistory обновляется;
- [x] Manager видит изменение;
- [x] Client видит публичное milestone-событие;
- [x] mobile UI usable на 375px.

---

## A4. Warehouse принимает груз

Warehouse:
1. переключается на Warehouse;
2. видит ожидаемую заявку;
3. ищет её по номеру;
4. открывает приёмку;
5. видит ожидаемый вес / места;
6. вводит фактические данные;
7. добавляет фото;
8. подтверждает приёмку.

Фото передаётся вместе с формой приёмки и связывается с созданной операцией `INTAKE`; при ошибке записи временная загрузка удаляется.

### Acceptance

- [x] создаётся WarehouseOperation `INTAKE`;
- [x] фиксируются фактические данные;
- [x] attachment сохраняется;
- [x] status → `AT_WAREHOUSE`;
- [x] Manager получает notification;
- [x] Client получает публичное обновление;
- [x] данные не пропадают после refresh.

---

## A5. Warehouse выполняет операции

Warehouse:
1. открывает груз;
2. переводит в `WAREHOUSE_PROCESSING`;
3. отмечает упаковку;
4. отмечает фотоотчёт;
5. добавляет результат;
6. переводит в `READY_FOR_DELIVERY`.

### Acceptance

- [x] OrderService completion сохраняется;
- [x] WarehouseOperation сохраняются;
- [x] тип, исполнитель, время, результат, заметка и измерения видны в журнале;
- [x] фото операции связано с WarehouseOperation;
- [x] timeline обновляется;
- [x] Manager видит `Готов к отправке`;
- [x] Client видит понятный публичный статус.

---

## A6. Manager запускает доставку

Manager:
1. открывает заявку;
2. назначает outbound driver / подтверждает существующего;
3. подтверждает автомобиль; до складской выдачи заявка остаётся в `READY_FOR_DELIVERY`.

Warehouse:
1. записывает получателя и количество мест;
2. указывает адрес доставки и при необходимости телефон получателя;
3. сверяет назначенных водителя и автомобиль;
4. при необходимости прикладывает фото подтверждения;
5. создаёт `WarehouseOperation.RELEASE` и отдельную точку маршрута `DELIVERY`.

Driver:
1. получает назначение;
2. начинает доставку;
3. status → `DELIVERY_IN_PROGRESS`;
4. доставляет;
5. прикладывает Proof of Delivery;
6. status → `DELIVERED`.

Сервер не разрешает водителю установить `DELIVERED` без клиентского файла категории `PROOF_OF_DELIVERY`; интерфейс показывает путь к загрузке и блокирует действие до появления файла.

Manager:
1. проверяет;
2. завершает заявку;
3. status → `COMPLETED`.

### Acceptance

- [x] полный lifecycle валиден;
- [x] операция выдачи содержит получателя, водителя, автомобиль, количество, время и комментарий;
- [x] исходящий адрес сохранён отдельной точкой `DELIVERY` и показан назначенному водителю;
- [x] фото выдачи связано с операцией и доступно клиенту;
- [x] proof attachment связан с Order;
- [x] Client видит завершение;
- [x] `completed_at` установлен;
- [x] после COMPLETED обычный Driver не может менять status;
- [x] документы доступны по permissions.

---

# 3. Demo Flow B — Прямая доставка своим транспортом

Order type:
`DELIVERY_OWN_TRANSPORT`

Flow:

```text
DRAFT
→ SUBMITTED
→ REVIEW
→ CONFIRMED
→ DRIVER_ASSIGNED
→ PICKUP_IN_PROGRESS
→ PICKED_UP
→ DELIVERY_IN_PROGRESS
→ DELIVERED
→ COMPLETED
```

### Acceptance

- [x] складские статусы не обязательны;
- [x] Warehouse не получает нерелевантную заявку;
- [x] Driver видит pickup + delivery;
- [x] Client видит маршрут и timeline;
- [x] Manager может завершить процесс.

Фильтрация проверена на локальной PostgreSQL в Flow B: прямая заявка не назначена складу, и склад не получил уведомление.

---

# 4. Demo Flow C — Транспортная компания

Order type:
`DELIVERY_TRANSPORT_COMPANY`

Проверить:
- sender;
- recipient;
- city;
- transport company;
- payer;
- packaging;
- insurance;
- documents.

### Acceptance

- [x] transport company сохраняется;
- [x] форма показывает только релевантные поля;
- [x] нет обязательного Driver, если собственный водитель не используется;
- [x] Order Details корректно отображает тип.

---

# 5. Demo Flow D — Документы / курьер

Order type:
`COURIER_DOCUMENTS`

Проверить:
- дата;
- время;
- получатель;
- телефон;
- адрес;
- количество экземпляров;
- возврат документов;
- attachments.

### Acceptance

- [x] грузовые поля не являются обязательными;
- [x] simplified wizard;
- [x] заявка видна Manager;
- [x] Client может отслеживать status.

---

# 6. Demo Flow E — Issue

Driver или Warehouse сообщает о проблеме.

Пример:
- ожидалось 5 мест;
- фактически 4.

### Acceptance

- [x] создаётся issue;
- [x] сервер требует описание проблемы и сохраняет его в истории статусов;
- [x] можно приложить фото;
- [x] Manager получает notification;
- [x] UI показывает IssueBanner;
- [x] Client не получает внутренние детали, если они internal;
- [x] после resolution lifecycle может продолжиться.

---

# 7. Demo Flow F — Permissions

Проверить вручную:

### Client
- [x] не видит чужую компанию;
- [x] не видит internal comments;
- [x] не назначает Driver;
- [x] не меняет operational status напрямую.

### Driver
- [x] не видит чужие рейсы;
- [x] не видит finance;
- [x] не видит Admin routes.

### Warehouse
- [x] не меняет цену;
- [x] не управляет users.

### Manager
- [x] не меняет роли пользователей.

### Admin
- [x] имеет полный административный доступ;
- [x] audit остаётся read-only.

---

# 8. Demo Role Switcher

Проверить:
- [x] доступны 5 ролей;
- [x] switch меняет current demo identity;
- [x] меняется navigation;
- [x] shared data остаются общими;
- [x] role switch не сбрасывает БД;
- [x] unauthorized route redirects / blocks appropriately.

---

# 9. Notifications Acceptance

После relevant events:
- [x] создаётся DB notification;
- [x] unread counter обновляется;
- [x] notification открывает Order;
- [x] mark as read работает.

Проверено на Flow B: уведомления клиента создавались по статусным событиям, счётчик уменьшился с 11 до 10 после отметки прочитанным, ссылка открыла нужную заявку. Для склада релевантность событий проверена в Flow A/B.

Минимальные события:
- order submitted;
- driver assigned;
- picked up;
- warehouse received;
- ready for delivery;
- delivered;
- issue.

---

# 10. Documents Acceptance

- [x] upload работает;
- [x] file metadata сохраняется;
- [x] preview / download работает;
- [ ] uploader виден;
- [x] category видна;
- [x] permissions работают;
- [ ] completed order не позволяет обычному пользователю бесследно удалить operational file.

---

# 11. Responsive Acceptance

Обязательные реальные проверки:

```text
375x812
390x844
430x932
768x1024
1024x768
1440x900
```

### На mobile
- [x] no horizontal overflow;
- [x] bottom nav видна;
- [x] sticky action не перекрывает контент;
- [ ] Create Order usable;
- [x] Driver flow usable;
- [ ] Warehouse intake usable;
- [ ] dialogs / drawers не выходят за viewport.

---

# 12. Build Acceptance

Обязательно проходят:

```bash
npm run lint
npm run typecheck
npm run build
```

Если есть tests:
```bash
npm test
```

---

# 13. Demo Reset

Если `DEMO_MODE=true`:
- [ ] можно вернуть seeded state;
- [x] reset защищён confirm;
- [x] reset недоступен в production mode.

Защитная фраза и production-блокировка подтверждены проверкой кода. Само восстановление seed в этом прогоне намеренно не запускалось, чтобы сохранить созданные и принятые демо-заявки; возможность полного восстановления базы остаётся без сквозной приёмки.

---

# 14. Финальный Demo Script

Рекомендуемый показ клиенту:

1. Client Dashboard.
2. Создать новую заявку.
3. Переключиться на Manager.
4. Принять заявку и назначить Driver.
5. Переключиться на Driver mobile.
6. Начать рейс и забрать груз.
7. Переключиться на Warehouse.
8. Принять груз и выполнить операцию.
9. Вернуться к Manager.
10. Отправить в доставку.
11. Driver подтверждает доставку.
12. Client открывает completed timeline.

Цель:
за 8–12 минут показать весь продукт как единую живую систему.
